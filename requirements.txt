# v0 core runs on the standard library alone — no dependencies required to run
# `python -m demo.run_session` or the pure-logic tests.

# Dev / test only:
pytest>=8.0

# Slack-phase seams (STEP 0):
mcp>=1.0            # engine wrapped as an MCP server (mcp_server/)
anthropic>=0.40    # real Claude/qwen mediator (mediator/claude.py)

# Harness (Phase A) only:
#   pandas, pyarrow  # to read the Habermas parquet

# Slack surface (slack_app/):
slack-bolt>=1.18
aiohttp>=3.9    # AsyncSocketModeHandler
